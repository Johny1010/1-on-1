import React from 'react';

import classes from './UserDiet.module.css';

// [to do] add product from filtered list (466, 467)

const UserDiet = (props) => {
    return(
        <div>Diet(user)</div>
    )
};

export default UserDiet;